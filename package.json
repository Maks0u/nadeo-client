{
  "name": "nadeo-client",
  "version": "1.0.0",
  "description": "",
  "author": "Maks0u",
  "license": "MIT",
  "type": "module",
  "main": "build/index.js",
  "files": [
    "build"
  ],
  "scripts": {
    "prestart": "tsc",
    "start": "node index.js",
    "build": "tsc && esbuild bundle.js --bundle --platform=node --minify --outfile=build/index.js"
  },
  "devDependencies": {
    "@tsconfig/node16": "^1.0.3",
    "@types/node": "^18.13.0",
    "esbuild": "^0.17.8",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "axios": "^1.3.3"
  }
}
